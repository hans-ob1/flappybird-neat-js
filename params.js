// get game canvas
var Params = {};

// game parameters
Params.Game = {
    NUM_PIPES: 100,
    PIPE_WIDTH: 50,
    PIPE_HEIGHT: 500,
    PIPE_MIN_Y: 100,
    PIPE_MAX_Y: 300,
    PIPE_SPEED: -3,

    GAP_HEIGHT: 100,

    INIT_BIRD_X: 100,
    INIT_BIRD_Y: 200,
    BIRD_RADIUS: 15,

    SIM_SPEED: 15
}

Params.DrawFrame = {
    FRAME_WIDTH: 336,
    FRAME_HEIGHT: 512,
    GROUND_POS: 495,

    SCORE_Y: 20,
    SCORE_WIDTH: 24,
    SCORE_SPACE: 2
}

Params.AI = {
    NUM_BIRDS: 1,

}